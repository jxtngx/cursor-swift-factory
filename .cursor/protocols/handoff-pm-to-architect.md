# Handoff: Product Manager → Chief Architect

Required attachments:

1. `.cursor/plans/project-init/<slug>-technical-requirements.plan.md`
2. `.cursor/plans/project-init/<slug>-platform.plan.md`
3. `TRACK.md` (`ios-iphone` | `ios-ipad` | `ios-universal` | `macos` | `watchos`)
4. User approval in the thread

Architect rejects the handoff if TRACK is unset, device class is missing for iOS, or the UI default is not SwiftUI without an ADR.
