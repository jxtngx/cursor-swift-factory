# Walking skeleton — macOS

Used only after the spec is approved and `TRACK.md` is `macos`.

Platform Engineer scaffolds:

- macOS SwiftUI app (`WindowGroup` or `DocumentGroup` if the spec is document-based)
- Swift 6
- SPM `Domain` package
- Menu extras only if the spec listed them as MVP
- `swift test` / macOS test target without Developer ID secrets

Do not add an iOS target in this milestone.
