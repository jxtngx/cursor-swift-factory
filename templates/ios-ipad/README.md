# Walking skeleton — iOS iPad

Used only after the spec is approved and `TRACK.md` is `ios-ipad`.

Platform Engineer scaffolds:

- Xcode app target, iPad destination
- Swift 6, SwiftUI `WindowGroup`
- Navigation that can grow into sidebar / split (even if MVP is a single column)
- SPM `Domain` package
- Test target on iPad simulator, no signing secrets

Do not treat compact iPhone as in-scope unless TRACK is changed to `ios-universal`.
