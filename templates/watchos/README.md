# Walking skeleton — watchOS

Used only after the spec is approved and `TRACK.md` is `watchos`.

Platform Engineer scaffolds:

- Watch App SwiftUI lifecycle
- Swift 6
- SPM `Domain` if the domain is shareable; otherwise Watch-only sources
- One screen + optional placeholder complication if the spec marked it must-have
- Tests that do not require a paired iPhone or paid signing

An iPhone companion is **out of TRACK** unless a later approved spec adds it.
