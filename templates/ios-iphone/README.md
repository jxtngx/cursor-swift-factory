# Walking skeleton — iOS iPhone

Used only after the spec is approved and `TRACK.md` is `ios-iphone`.

Platform Engineer scaffolds:

- Xcode app target, iPhone destination (not iPad-only layouts)
- Swift 6, SwiftUI `WindowGroup`
- SPM `Domain` package the app depends on
- One screen: app name + placeholder from the spec
- Test target that runs on the iPhone simulator without signing secrets

Do not add iPad multitasking or Watch targets here.
